apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "NCFK.fullname" . }}-test-connection"
  labels:
    {{- include "NCFK.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "NCFK.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
